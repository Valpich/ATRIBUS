package fr.eseo.atribus.entities;

import java.io.Serializable;

/**
 * The Class UtilisateurLdap.
 */
public class UtilisateurLdap implements Serializable {

  /** La constante serialVersionUID. */
  private static final long serialVersionUID = 8177715403753444254L;

  /** La variable cn. */
  private String cn;

  /** La variable mail. */
  private String mail;

  /** La variable given name. */
  private String givenName;

  /** La variable object class. */
  private String objectClass;

  /** La variable userpassword. */
  private String userpassword;

  /** La variable sn. */
  private String sn;

  /** La variable uid. */
  private String uid;

  /**
   * Instancie un nouveau utilisateur ldap.
   */
  public UtilisateurLdap() {
    super();
  }

  /**
   * Accesseur en lecture sur le paramètre cn.
   *
   * @return le paramètre cn
   */
  public String getCn() {
    return this.cn;
  }

  /**
   * Accesseur en écriture sur le paramètre cn.
   *
   * @param cn le nouveau paramètre cn
   */
  public void setCn(String cn) {
    this.cn = cn;
  }

  /**
   * Accesseur en lecture sur le paramètre mail.
   *
   * @return le paramètre mail
   */
  public String getMail() {
    return this.mail;
  }

  /**
   * Accesseur en écriture sur le paramètre mail.
   *
   * @param mail le nouveau paramètre mail
   */
  public void setMail(String mail) {
    this.mail = mail;
  }

  /**
   * Accesseur en lecture sur le paramètre given name.
   *
   * @return le paramètre given name
   */
  public String getGivenName() {
    return this.givenName;
  }

  /**
   * Accesseur en écriture sur le paramètre given name.
   *
   * @param givenName le nouveau paramètre given name
   */
  public void setGivenName(String givenName) {
    this.givenName = givenName;
  }

  /**
   * Accesseur en lecture sur le paramètre object class.
   *
   * @return le paramètre object class
   */
  public String getObjectClass() {
    return this.objectClass;
  }

  /**
   * Accesseur en écriture sur le paramètre object class.
   *
   * @param objectClass le nouveau paramètre object class
   */
  public void setObjectClass(String objectClass) {
    this.objectClass = objectClass;
  }

  /**
   * Accesseur en lecture sur le paramètre userpassword.
   *
   * @return le paramètre userpassword
   */
  public String getUserpassword() {
    return this.userpassword;
  }

  /**
   * Accesseur en écriture sur le paramètre userpassword.
   *
   * @param userpassword le nouveau paramètre userpassword
   */
  public void setUserpassword(String userpassword) {
    this.userpassword = userpassword;
  }

  /**
   * Accesseur en lecture sur le paramètre sn.
   *
   * @return le paramètre sn
   */
  public String getSn() {
    return this.sn;
  }

  /**
   * Accesseur en écriture sur le paramètre sn.
   *
   * @param sn le nouveau paramètre sn
   */
  public void setSn(String sn) {
    this.sn = sn;
  }

  /**
   * Accesseur en lecture sur le paramètre uid.
   *
   * @return le paramètre uid
   */
  public String getUid() {
    return this.uid;
  }

  /**
   * Accesseur en écriture sur le paramètre uid.
   *
   * @param uid le nouveau paramètre uid
   */
  public void setUid(String uid) {
    this.uid = uid;
  }

  /*
   * (non-Javadoc)
   * 
   * @see java.lang.Object#toString()
   */
  @Override
  public String toString() {
    return "UtilisateurLdap [cn=" + this.cn + ", mail=" + this.mail + ", givenName="
        + this.givenName + ", objectClass=" + this.objectClass + ", userpassword="
        + this.userpassword + ", sn=" + this.sn + ", uid=" + this.uid + "]";
  }
}
