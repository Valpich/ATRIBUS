package fr.eseo.atribus.entities;

import java.io.Serializable;
import java.util.Map;

/**
 * The Class UniteEnseignement.
 */
public class UniteEnseignement implements Serializable {

  /** La constante serialVersionUID. */
  private static final long serialVersionUID = 2235338214834313313L;

  /** La variable id. */
  private int id;

  /** La variable nom. */
  private String nom;

  /** La variable nb credits ects. */
  private int nbCreditsEcts;

  /** La variable nb heures. */
  private int nbHeures;

  /** La variable semestre. */
  private Semestre semestre;

  /** La variable enseignant ref ue. */
  private EnseignantRefUe enseignantRefUe;

  /** La variable competences. */
  private Map<Long, CompetenceValidable> competences;

  /**
   * Accesseur en lecture sur le paramètre id.
   *
   * @return le paramètre id
   */
  public int getId() {
    return this.id;
  }

  /**
   * Accesseur en écriture sur le paramètre id.
   *
   * @param id le nouveau paramètre id
   */
  public void setId(int id) {
    this.id = id;
  }

  /**
   * Accesseur en lecture sur le paramètre nom.
   *
   * @return le paramètre nom
   */
  public String getNom() {
    return this.nom;
  }

  /**
   * Accesseur en lecture sur le paramètre nb credits ects.
   *
   * @return le paramètre nb credits ects
   */
  public int getNbCreditsEcts() {
    return this.nbCreditsEcts;
  }

  /**
   * Accesseur en lecture sur le paramètre nb heures.
   *
   * @return le paramètre nb heures
   */
  public int getNbHeures() {
    return this.nbHeures;
  }

  /**
   * Accesseur en lecture sur le paramètre enseignant ref ue.
   *
   * @return le paramètre enseignant ref ue
   */
  public EnseignantRefUe getEnseignantRefUe() {
    return this.enseignantRefUe;
  }

  /**
   * Accesseur en lecture sur le paramètre semestre.
   *
   * @return le paramètre semestre
   */
  public Semestre getSemestre() {
    return this.semestre;
  }

  /**
   * Accesseur en lecture sur le paramètre competences.
   *
   * @return le paramètre competences
   */
  public Map<Long, CompetenceValidable> getCompetences() {
    return this.competences;
  }

  /**
   * Accesseur en écriture sur le paramètre nom.
   *
   * @param nom le nouveau paramètre nom
   */
  public void setNom(String nom) {
    this.nom = nom;
  }

  /**
   * Accesseur en écriture sur le paramètre nb credits ects.
   *
   * @param nbCreditsEcts le nouveau paramètre nb credits ects
   */
  public void setNbCreditsEcts(int nbCreditsEcts) {
    this.nbCreditsEcts = nbCreditsEcts;
  }

  /**
   * Accesseur en écriture sur le paramètre nb heures.
   *
   * @param nbHeures le nouveau paramètre nb heures
   */
  public void setNbHeures(int nbHeures) {
    this.nbHeures = nbHeures;
  }

  /**
   * Accesseur en écriture sur le paramètre enseignant ref ue.
   *
   * @param enseignantRefUe le nouveau paramètre enseignant ref ue
   */
  public void setEnseignantRefUe(EnseignantRefUe enseignantRefUe) {
    this.enseignantRefUe = enseignantRefUe;
  }

  /**
   * Accesseur en écriture sur le paramètre semestre.
   *
   * @param semestre le nouveau paramètre semestre
   */
  public void setSemestre(Semestre semestre) {
    this.semestre = semestre;
  }

  /**
   * Sets the competences.
   *
   * @param competences le competences
   */
  public void setCompetences(Map<Long, CompetenceValidable> competences) {
    this.competences = competences;
  }

  /*
   * (non-Javadoc)
   * 
   * @see java.lang.Object#toString()
   */
  @Override
  public String toString() {
    return "UniteEnseignement [id=" + this.id + ", nom=" + this.nom + ", nbCreditsEcts="
        + this.nbCreditsEcts + ", nbHeures=" + this.nbHeures + ", semestre=" + this.semestre
        + ", enseignantRefUe=" + this.enseignantRefUe + ", competences=" + this.competences
        + ", toString()=" + super.toString() + "]";
  }



}
