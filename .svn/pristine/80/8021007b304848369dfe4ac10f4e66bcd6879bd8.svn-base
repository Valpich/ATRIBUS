package fr.eseo.atribus.entities;

import java.io.Serializable;
import java.util.List;

/**
 * The Class Matiere.
 */
public class Matiere implements Serializable {

  /** La constante serialVersionUID. */
  private static final long serialVersionUID = 2590416927567525328L;

  /** La variable id. */
  private int id;

  /** La variable nom. */
  private String nom;

  /** La variable coefficient. */
  private float coefficient;

  /** La variable ue. */
  private UniteEnseignement ue;

  /** La variable semestre. */
  private Semestre semestre;

  /** La variable competences. */
  private List<CompetenceValidable> competences;

  /** La variable erm. */
  private EnseignantRefMatiere erm;

  /**
   * Accesseur en lecture sur le paramètre id.
   *
   * @return le paramètre id
   */
  public int getId() {
    return this.id;
  }

  /**
   * Accesseur en lecture sur le paramètre nom.
   *
   * @return le paramètre nom
   */
  public String getNom() {
    return this.nom;
  }

  /**
   * Accesseur en lecture sur le paramètre coefficient.
   *
   * @return le paramètre coefficient
   */
  public float getCoefficient() {
    return this.coefficient;
  }


  /**
   * Accesseur en écriture sur le paramètre id.
   *
   * @param id le nouveau paramètre id
   */
  public void setId(int id) {
    this.id = id;
  }

  /**
   * Accesseur en écriture sur le paramètre nom.
   *
   * @param nom le nouveau paramètre nom
   */
  public void setNom(String nom) {
    this.nom = nom;
  }

  /**
   * Accesseur en écriture sur le paramètre coefficient.
   *
   * @param coefficient le nouveau paramètre coefficient
   */
  public void setCoefficient(float coefficient) {
    this.coefficient = coefficient;
  }

  /**
   * Accesseur en lecture sur le paramètre ue.
   *
   * @return le paramètre ue
   */
  public UniteEnseignement getUe() {
    return this.ue;
  }

  /**
   * Accesseur en écriture sur le paramètre ue.
   *
   * @param ue le nouveau paramètre ue
   */
  public void setUe(UniteEnseignement ue) {
    this.ue = ue;
  }

  /**
   * Accesseur en lecture sur le paramètre semestre.
   *
   * @return le paramètre semestre
   */
  public Semestre getSemestre() {
    return this.semestre;
  }

  /**
   * Accesseur en écriture sur le paramètre semestre.
   *
   * @param semestre le nouveau paramètre semestre
   */
  public void setSemestre(Semestre semestre) {
    this.semestre = semestre;
  }

  /**
   * Accesseur en lecture sur le paramètre competences.
   *
   * @return le paramètre competences
   */
  public List<CompetenceValidable> getCompetences() {
    return this.competences;
  }

  /**
   * Accesseur en écriture sur le paramètre competences.
   *
   * @param competences le nouveau paramètre competences
   */
  public void setCompetences(List<CompetenceValidable> competences) {
    this.competences = competences;
  }

  /**
   * Accesseur en lecture sur le paramètre erm.
   *
   * @return le paramètre erm
   */
  public EnseignantRefMatiere getErm() {
    return this.erm;
  }

  /**
   * Accesseur en écriture sur le paramètre erm.
   *
   * @param erm le nouveau paramètre erm
   */
  public void setErm(EnseignantRefMatiere erm) {
    this.erm = erm;
  }

  /*
   * (non-Javadoc)
   * 
   * @see java.lang.Object#toString()
   */
  @Override
  public String toString() {
    return "Matiere [id=" + this.id + ", nom=" + this.nom + ", coefficient=" + this.coefficient
        + ", ue=" + this.ue + ", semestre=" + this.semestre + ", competences=" + this.competences
        + "]";
  }
}
